#include<stdio.h>
#include<string.h>
#include<stdlib.h>
void main()
{
    char a[30],b[30],n,c[100];
    int i,j;
    printf("Enter first string: ");
    gets(a);
    printf("Enter second string: ");
    gets(a);
    for(i=0;a[i]!='\0';i++)
    {
        for(j=0;b[j]!='\0';j++)
        {
            if(a[i]!=b[j])
            {

            }
        }
    }
}
